odoo:
  image: cutech/docker-odoo-11
  ports:
    - ${XXPORT}:${IXPORT}
    - ${XXSPORT}:${IXSPORT}
    - ${XLPPORT}:${ILPPORT}
  links:
    - db
db:
  image: postgres
  environment:
    - POSTGRES_USER=${PGUSER}
    - POSTGRES_PASSWORD=${PGPASS}
